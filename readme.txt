extract dir
navigate to dir using wsl
"make proj1" without quotes will compile the prog
"cd test" seems to work as expected
need to implement executing other programs and save the processid in array
maybe make a helper function to accept the pid and it will save into the array
poping or pushing (its late) so we always have the last 5 for the other built in function
I have examples of forking procs from sysprog class in summer but need to find
would be cool to update the verbose (extra print statements) to only output when
we pass through a -verbose argument when running ./proj1 -verbose or something
lmk if something isn't clear